{
  "name": "personal-site",
  "version": "1.0.0",
  "type": "module",
  "description": "Personal portfolio website",
  "scripts": {
    "build": "yarn build:blog && yarn build:og && yarn build:meta",
    "build:blog": "node scripts/build-blog.js",
    "build:meta": "node scripts/update-meta-tags.js",
    "build:og": "node scripts/generate-og-images.js",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --check .",
    "format:fix": "prettier --write ."
  },
  "devDependencies": {
    "@eslint/js": "9.0.0",
    "eslint": "9.0.0",
    "eslint-config-prettier": "9.0.0",
    "fast-glob": "3.3.0",
    "gray-matter": "4.0.3",
    "husky": "9.1.7",
    "lint-staged": "15.0.0",
    "marked": "12.0.0",
    "prettier": "3.0.0",
    "satori": "0.10.9",
    "sharp": "0.33.0"
  },
  "lint-staged": {
    "*.{html,css,md}": [
      "prettier --write",
      "git add"
    ],
    "*.js": [
      "eslint --fix",
      "prettier --write",
      "git add"
    ]
  }
}
