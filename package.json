{
  "name": "personal-site",
  "version": "4.0.0",
  "type": "module",
  "description": "Static personal website",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -R src/. dist/ && cp -R public/. dist/ && node scripts/build-content.js && node scripts/write-deploy-time.js",
    "dev": "concurrently \"nodemon --watch src -e html,css,js,md --exec 'yarn build'\" \"serve dist\"",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --check .",
    "format:fix": "prettier --write .",
    "prepare": "husky"
  },
  "devDependencies": {
    "@eslint/js": "9.0.0",
    "concurrently": "9.2.1",
    "eslint": "9.0.0",
    "eslint-config-prettier": "9.0.0",
    "husky": "9.1.7",
    "lint-staged": "15.0.0",
    "nodemon": "3.1.14",
    "prettier": "3.0.0",
    "serve": "14.2.6"
  },
  "lint-staged": {
    "*.{html,css,md}": [
      "prettier --write"
    ],
    "*.js": [
      "eslint --fix",
      "prettier --write"
    ]
  }
}
