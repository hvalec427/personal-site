{
  "outputDirectory": "_site",
  "buildCommand": "yarn gallery:process && bundle exec jekyll build --config _config.yml,_config.${JEKYLL_ENV:-production}.yml",
  "installCommand": "gem install bundler && yarn install",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    },
    {
      "status": 404,
      "src": "/(.*)",
      "dest": "/404.html"
    }
  ]
}
